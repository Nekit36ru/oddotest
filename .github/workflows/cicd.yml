name: CICD
on:
  push:
    branches:
      - master
jobs:
  test:
    name: deploy only changed-files
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Run command on remote server
       command:
            ls -l
            /home/oddotest1/cicd.sh;
