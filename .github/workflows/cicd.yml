name: CICD
on:
  push:
    branches:
      - master
jobs:
  test:
    name: deploy only changed-files
    runs-on: ubuntu-latest
    steps:
    - name: Checkout the code
      uses: actions/checkout@v3
    name: clon repo
    uses: git@github.com:Nekit36ru/oddotest.git    
    name: Install 
    run: ls -la
   # - name: Install 
   #    run: ls -l
   #    run: ip a
